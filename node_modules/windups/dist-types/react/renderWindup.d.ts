import React from "react";
import { ChildrenMetadata, ChildrenWindup } from "./WindupChildren";
import { PlayedWindup } from "../Windup";
import { StringMetadata, StringWindup } from "./useWindupString";
export declare type PlayedChildrenWindup = PlayedWindup<string, ChildrenMetadata>;
export declare type PlayedStringWindup = PlayedWindup<string, StringMetadata>;
export declare function renderStringWindup(windup: StringWindup | PlayedStringWindup): string;
export declare function renderChildrenWindup(windup: ChildrenWindup | PlayedChildrenWindup): React.ReactNode;
